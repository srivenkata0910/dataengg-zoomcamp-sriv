version: 2

seeds:
  - name: employees
    config:
      schema: seed_data
    description: a manual map of employees to customers
    columns:
      - name: employee_id
        tests:
          - unique
          - not_null